{% extends 'base.html.twig' %}

{% block title %}
Réinitialiser le mot de passe ◈ Esca'Lab
{% endblock %}

{% block body %}
<main class="gradient-light-grey-light-blue">
  <div class="container min-vh-100">
    <div class="row min-vh-100">
      <div class="col-xl-3"></div>
      <div class="col-xl-6 align-self-center">
        <form class="custom-container"
          method="post"
          action="/connexion/mot-de-passe-oublie">
          <div class="text-center">
            <img src="{{ asset('images/logo.png') }}"
              width="50"
              height="50"
              class="rounded"
              alt="logo" />
          </div>

          <h1 class="text-center lobster text-black mb-3">
            Mot de passe oublié
          </h1>
          <p class="fst-italic fs-6 poppins light mb-3">
            Vous avez oublié votre mot de passe ? Pas de soucis, il vous
            suffit d'entrer votre <span class="poppins medium">adresse mail</span> ou votre <span
              class="poppins medium">pseudo</span>. Vous recevrez
            un email avec <span class="poppins medium">un code a usage unique</span> vous permettant de
            réinitialiser votre mot de passe.
          </p>
          <div class="mb-3">
            <label for="email"
              class="form-label poppins light">
              Adresse email ou pseudo
            </label>
            <input type="emailOrUsername"
              class="form-control poppins"
              name="emailOrUsername"
              required
              value="{{ emailOrUsername ?? '' }}"
              id="emailOrUsername"
              placeholder="john.doe@domaine.fr - johndoe123" />
          </div>
          {% if error.not_exists is defined %}
          <p class="fst-italic text-danger poppins bold tiny">Désolé, mais cet utilisateur n'héxiste pas.</p>
          {% endif %}
          <div class="text-center">
            <input type="hidden"
              name="passwordReset"
              value="reset" />
            <button class="btn btn-primary poppins bold mb-3 rounded"
              type="submit">
              Réinitialiser
            </button>
            <p class="fst-italic fs-6 text-muted poppins light">
              Vous vous souvenez de votre mot de passe ? <a class="text-decoration-none"
                href="{{ path('login') }}">Connectez-vous</a>
            </p>
          </div>
        </form>
      </div>
      <div class="col-xl-3"></div>
    </div>
  </div>
</main>
{% endblock %}